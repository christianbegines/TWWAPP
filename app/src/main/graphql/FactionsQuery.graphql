query FactionsQuery($tww_version: String!) {
  tww(tww_version: $tww_version) {
    tww_version
    factions(include_non_mp: false) {
      key
      screen_name
      subculture {
        name
        __typename
      }
      flags_url
    }
    __typename
  }
}
